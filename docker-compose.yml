services:
  app:
    build: .
    image: yashpandav1997/wealth-management:latest

    restart: unless-stopped
    
    env_file:
      - .env

    ports:
      - "3000:3000"

    volumes:
      - uploads:/app/public/uploads

volumes:
  uploads: